##compile help：
# Using CMAKE 
default use TDM-GCC64 tools
switch to CMakeList.txt dir and create "build" dir
1$ cd build
2$ cmake .. -G "MinGW Makefiles"     //Windows platform
3$ make -j #or mingw32-make according to your environment

####################################################################
Note：
1.revise CMakeList.txt according to different samples
2.copy the right version libdmcam.dll to excute dir.
####################################################################
